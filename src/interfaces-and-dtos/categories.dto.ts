// the DTOs are usually taken from the dev code libraries in my experience and auto-updated
// but for the sake of demonstration on correct tryping I added them manually

export type CategoryDTO = {
    slug: string;
    name: string;
    url: string;
};
